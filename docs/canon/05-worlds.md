# 05-worlds.md — the settled worlds & the dark between

> **Cornerstone file — the physical setting: where humanity lives and the space between.**
> See `docs/canon/README.md` for the authoring conventions (numbering, the three structured
> headings, fact ids).
>
> **STATUS: scaffold (metadata only — not yet authored).** Everything below sits *above* the
> first `##` heading on purpose, so the seeder treats this file as empty: none of this guidance
> reaches the DJs' context until you write real `## Section` prose and `## Canon facts`. It is a
> brief for *you*, the author.

**What this covers** — the "map" of the world: the settled worlds and clusters, the routes ships
take, the dark between them, and where the drifting relay-station sits within it all. Distance is
the world's defining fact — travel takes weeks — so this file establishes the *scale* every other
file lives inside.

**Building blocks to consider**
- The major worlds / clusters and the character of each (a harbour world, a frontier world…).
- The routes between them and roughly how long each crossing takes.
- Where the station drifts — "always between," never quite anywhere (cf. `00-station.md`).
- Core vs. frontier; what lies beyond the settled edge; the unmapped dark.
- "The between" treated as a *place*, not just empty space.

**The tribute** — the cartographer's wonder of the genre: deep space as a setting that *shapes*
society, distance as a social force. Reference the tradition, never any franchise's map or names.

**When you author this file:** add narrative `## <Topic>` sections (they become the DJs' cached
"series bible" prose) and a `## Canon facts` list of the hard, queryable facts (`canon-worlds-N`).
Optionally tag facts (`- **Tags:** …`).

## Canon facts

1. The settled worlds fall loosely into a dense core of long-established worlds and a thin frontier at the settled edge, with the unmapped dark beyond.
   - **Tags:** worlds, core, frontier, dark, scale, settlements
2. Neighbouring worlds lie a journey of weeks apart; the dark between is treated as a place in its own right, not mere empty space.
   - **Tags:** distance, between, dark, travel, scale, isolation
3. The station holds no fixed world but drifts along the relay routes, which is why it can speak to all of them and belong to none.
   - **Tags:** station, relay, between, neutrality, route
