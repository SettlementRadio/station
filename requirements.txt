# Core pipeline (all phases).
anthropic>=0.40
elevenlabs>=1.0
python-dotenv>=1.0
requests>=2.31

# Local TTS — Kokoro (default TTS_PROVIDER from B0). Self-hosted, free,
# unlimited. Requires Python 3.10–3.12 (NOT 3.13 yet) and the espeak-ng system
# package; see README "Kokoro (local TTS)" for the full install + the model
# download. soundfile writes Kokoro's 24 kHz output to WAV before the shared
# _to_mp3() transcode.
kokoro>=0.9.4
soundfile>=0.12

# World-state database (B1): the queryable store behind src/world/store.py.
# psycopg 3 (the modern Postgres driver); [binary] ships prebuilt wheels so no
# local libpq/compiler is needed. pgvector is intentionally NOT added yet —
# vector search is deferred to B3 (see store.py / README).
psycopg[binary]>=3.1

# Foundation (B0.5): typed settings + structured logging.
# pydantic-settings backs the single src/config.py module (env/.env -> typed
# Settings); structlog is the one configured logger (JSON for 24/7 runs).
pydantic-settings>=2.0
structlog>=24.1

# Dev tooling (B0.5) — lint/format + the pre-commit gate. Not needed at runtime;
# installed in the dev venv. See README "Engineering standards / pre-commit".
ruff>=0.8
pre-commit>=4.0
pytest>=8.0  # surgical tests (run with: .venv/bin/python -m pytest)
