"""The writers' room (Layer 3) — the Claude steps that produce a script.

Phase A shipped a single-DJ writer at `src/writer.py`. Phase B's B4 adds the
two-DJ conversation here as a small package, so the room can grow more roles
(showrunner, continuity, formats) without crowding that one module.
"""
